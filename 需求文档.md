# 总体说明
本需求文档描述了LLM GW的功能、架构和测试要求。
LLM Gateway，简称LLM GW，是指一个基于LLM的网关，用于提供LLM的接口服务。本项目是要实现一个LLM GW，用于提供LLM的接口服务。

使用语言为python

# user story
1. 用户可以在本LLMGW支持的大模型供应商中定义多个大语言模型。
2. 用户有自己的后端agent。agent 通过LLMGW接入和使用LLM大模型。并且不管使用哪个大模型，从用户自己的agent 来看，输入和输出，以及权限管控都是统一的。大模型的不同特性，接口要求等都被LLM GW 处理掉了。
3. LLMGW 支持路由功能。可以根据当时的任务，模型健康，成本，用户选择等原因针对不同任务进行不同的路由
4. 用户可以通过LLMGW的可视化界面，查看历史请求记录。token消耗，请求时间，响应时间等。

# 架构设计
第一层：业务与交互层
这一层负责提供用户使用的web界面。用户可以在web 界面上定义大语言模型，查看可视化记录，各个模型状态，提供接入信息，同时一共一个简单的web chat 功能。
第二层：adapter 层
这一层负责对接不同大语言模型。适配不同的大模型。将不同的大模型抽象成统一的接口。同时提供错误处理机制。
第三层：路由层
这一层负责根据用户的任务，模型健康，成本，用户选择等原因针对不同任务进行不同的路由。同时提供路由策略的配置接口。
第四层：Harness层
提供一个接口服务，让后端的agent连接。


LLM ------ adapter ------ 路由层 ------ Harness层 ------- service


运行时把业务与交互层的 Web 控制台与 Harness 层的接口服务装配在同一个进程，因此同一个服务地址上既能访问面向前端页面的 `/api/*`，也能访问面向后端 agent 的 `/health` 与 `/v1/tasks`（含流式 `/v1/tasks:stream`）。


# 功能说明
## 业务与交互层功能
1. 提供web界面。web界面使用fastapi成熟的企业级web框架。所有大的功能集中在左侧导航中，共五个入口：模型定义、Profile、Chat、Dashboard、Trace。每个入口下的配置是单独的子页面。
2. 用户可以在web 界面上新增模型。默认配置项目有模型的供应商，模型（例如gpt-3.5-turbo， deepseek v4 flash），API key 等信息。可选默认配置项有 模型tag和展示名称。高级配置项目有模型的URL，输入和输出的上下文窗口，工具调用轮数，思考模式（跟随模型默认，开启，关闭），temperature，top_p， top_k 等。
3. 提供一个抽象的gwprofile层。通过web界面进行配置。这个gwprofile包括包含哪些定义的模型。可以配置一个在profile内生效的统一模型高级配置项模版。如果启用模版，那么在profile内的模型，如果不勾选“本模型配置优先于模版”，则LLMGW在使用这个模型时会忽略模型的高级配置项而专用模版中的配置。gwprofile层还提供路由配置。路由有两种选项。1，动态路由，2，静态路由。静态路由写死各个模型的优先顺序，用逗号分隔。
4. 提供一个简单的LLM chat 功能
5. 提供可视化dashboard。里面包括所有配置的大语言模型的可用状态，使用次数，使用token数。剩余token数。
6. 提供可视化trace页面。可以查看，搜索所有的prompt 日志。结构化展示。可以关键字搜索。可以按照不同agent的task进行图形化展示。

## adapter层功能
1. 定义统一的task schema。LLM GW和后端agent 都应该遵循这个schema。对收到的task 进行2中校验。1. 校验语法。2. 校验task 是否符合schema。
2. 将统一的task schema，翻译成不同大模型使用的prompt schema 并发送给LLM。
3. 将LLM的响应翻译成统一的task schema。并进行2层校验。1. 校验语法。2. 校验task 是否符合schema。
4. 向后端模块隐藏大模型的细节。只暴露统一的task schema。包括模型名称，token 等信息。
5. 每次LLM调用都记录好一下信息并返回给路由层。

| 维度   | 关键字段                                        |
| ------ | ----------------------------------------------- |
| 关联   | trace_id, run_id, step_id, call_id              |
| Prompt | 名称，版本，hash，Schema 版本                   |
| 用量   | input，output，cached，reasoning tokens         |
| 延迟   | queue，route， TTFT， generation，total latency |
| 弹性   | attempt，retry，fallback，timeout budget        |
| 结果   | finish reason，终态，输出校验结果               |
| 错误   | 稳定错误码，HTTP状态，供应商请求ID              |
| 成本   | 输入，输出，缓存及总估算成本                    |

需要注意的如下
- 针对并针对各个字段进行脱敏
- TTFT 必须以第一个有业务意义的detail为准。不能把连接建立事件作为首token
- Metrics 聚合趋势（QPS，P99，错误率），Logs记录单次调用细节，Trace 串联调用链路

7. 注意adapter层默认采用SSE方式和LLM进行通讯。
8. 在处理LLM输出的时候注意区分流式输出和结构化输出的在处理时的区别。
- 普通调用执行器
  同步请求 上游 完整相应 翻译回统一结果。最简单的链路，也是fallback和重试策略的基准
- streaming的内部事件
  gateway 内部维护事件序列（delta / tool_call / usage / done ），对外统一为SSE 协议
- 流式响应必须只有一个终态
  流式结束只有一个原因：正常完成，错误，或者取消。不能在流中间切换终态类型。这里需要注意的是流式如果是客户端连接断开，这个只是发的一个取消信号。可能下游模型的输出其实还在持续。
- structured output
  通过两层进行保证
  - 第一层：请求时带JSON schema
  - 第二层：返回后用Pydantic 本地进行schema校验。供应商的schema 保证不等于应用层就安全不报错。
- structured Steaming 的边界
  - 流式JSON 需要增量解析，不能登全部 chunk到齐再解析。但部分JSON无法做Schema校验，需在流救赎后做最终校验
- 输出修复要有边界
  - JSON 提取和修复只做有限次数的尝试。不能无限尝试。修复失败应返回明确错误，不是静默吞掉

## 路由模块功能
1. 路由模块提供模型能力注册表。记录包括模型的名称，供应商，是否支持SSE，是否支持流式输出等能力。
2. 路由默认提供动态路由。根据模型可用性，task的特征，消费比，能力注册表动态选择模型。
3. 路由模块也提供静态路由。用户可以在web界面配置静态路由。静态路由优先于动态路由。
4. 只要有能力的时候，都提供主备两个路由。在主路由无法使用的时候使用备用路由。
5. 每次路由选择都记录好一下信息并返回给Harness层

| 维度   | 关键字段                                        |
| ------ | ----------------------------------------------- |
| 路由   | 逻辑模型，供应商，实际模型，候选，拒绝原因，路由原因      |

## Harness层功能
1. 提供一个接口服务，让后端的agent连接。接口部分需要提供鉴权方式。支持简单的password
2. 收集adapter层和路由层返回的日志，进行分析和存储。
3. 对web界面提供日志查询功能的接口。
4. 提供错误处理机制。有些错误是可重试的，有些是不可重试的。有些需要警醒降级处理。harness要根据收集到的错误情况进行判断并在重试，降级，报错三者中选择一个进行。并妥善记录。

| 阶段       | 典型错误               | 是否适合重试        | 处理策略          |
| ---------- | ---------------------- | ------------------- | ------------------- |
| 认证       | API Key无效，租户禁用  | 否                  | 如果路由表中还有下一个模型，就更换路由表中下一个模型，并报错，但不阻塞。如果已经没有其他模型，则返回错误      |
| 请求验证   | 参数非法，Schema 缺失  | 否                  | 直接返回错误，不重试      |
| Prompt     | 缺变量，超过预算       | 修正请求后重试      | 修正请求后重试。达到最大重试次数后，按照路由更换模型再重试。      |
| 路由       | 无兼容模型             | 配置变化后重试      | 配置变化后重试。达到最大重试次数后，按照路由更换模型再重试。      |
| 排队       | 并发已满，截止时间不足 | 可拒绝或换池        | 可拒绝或换池      |
| 连接       | 网络抖动，短暂5xx      | 有限重试            | 有限重试，达到最大重试次数后，按照路由更换模型再重试。      |
| 首Token前  | 429，超时，过载        | 有限重试或fall back | 有限重试或fall back，达到最大重试次数后，按照路由更换模型再重试。      |
| 已流式输出 | 连接中断               | 不盲目重新生成      | 参考后面的描述      |
| 输出校验   | JSON/Schema错误        | 有限修复            | 有限修复，达到最大重试次数后，按照路由更换模型再重试。      |
| 内容拒答   | 安全策略拒答           | 更换模型重试。但是每个模型最多尝试一次     | 更换模型重试。但是每个模型最多尝试一次       |

其中的已流式输出的情况按照如下处理：
flowchart TD
    A["开始：流式请求"] --> B["Gateway 调用 LLM 流式接口"]
    B --> C{"流是否正常完成？<br/>收到 finish_reason / [DONE] / message_stop"}
    C -->|是| D["向客户端发送完成事件 / [DONE]"]
    D --> E["记录日志、计费、结束"]
    C -->|否| F{"中断来源？"}

    F -->|客户端断开| G["取消上游 LLM 请求"]
    G --> H["清理资源、释放并发槽"]
    H --> I["记录日志，结束"]

    F -->|上游 LLM 中断| J{"Gateway 是否已向客户端发送过数据？"}
    J -->|否| K{"是否可重试错误？<br/>网络 / 5xx / 429"}
    K -->|否| L["向客户端返回错误"]
    K -->|是| M{"是否有已执行的不可逆副作用？<br/>如下单、扣款、发邮件"}
    M -->|是| N["不能简单重试；走幂等 / 补偿 / 报错"]
    M -->|否| O["有限重试上游"]
    O --> B
    N --> L
    L --> P["记录日志、结束"]

    J -->|是| Q["不能透明重试整个请求"]
    Q --> R["发送流内 error 事件<br/>不发送 [DONE]"]
    R --> S["关闭连接"]
    S --> T["记录日志：request_id、已发送字节、最后 chunk、错误"]
    T --> U["结束；由客户端决定重试 / 接受部分内容"]
其中最大重试次数可以在web界面配置。默认为3.


# 生成说明
项目参考代码为 ./pi文件夹中的llm gateway部分代码。
使用git 进行版本控制。
1. 使用python 作为开发语言
2. 开发的时候使用SDD和TDD
3. 先进行测试用例的编写。再进行代码编写
4. 测试的时候使用mocktransport 模拟LLM的输出。
5. 测试内容至少包含如下类别 

| 测试类别              | 测试内容                                                     |
| --------------------- | ------------------------------------------------------------ |
| Adapter Contract Test | 测试每个adapter 的请求翻译和响应翻译是否符合协议用mocktransport驱动，不依赖真实供应商 |
| router测试            | 验证路由策略的如下功能：能力匹配，优先级，fallbcak顺序，候选拒绝原因是否正确记录 |
| 重试测试              | 用mocktransport模拟超时和错误，验证重试次数和退避策略，但不能真的sleep |
| streaming测试         | 验证SSE事件序列的正确性：delta顺序，usage 事件，终态，错误中断行为 |
| structured output测试 | 验证JSON提取，Scheme校验，修复尝试次数，校验失败时的错误返回 |
| 可观测性              | 验证trace，Metrics，Cost Ledger是否正确记录了每次调用的关键字段 |

6. 提供意见启动脚本

# 交付说明
交付物包括
1. 包含注释的代码。特别要标注版本号。
2. 测试用例
3. 说明文档。包含架构说明，接口说明，错误码说明，重试策略说明，运行说明的readme。版本更新说明。
4. 测试证据


